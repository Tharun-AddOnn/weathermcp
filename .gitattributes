# Normalise to LF in the repository; this project is built on Linux (Docker,
# Netlify) as well as edited on Windows.
* text=auto eol=lf

# Keep binary/lock files out of diffs
package-lock.json -diff
